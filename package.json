{
  "//": [
     "Devs - remember that in the folio convention is that the version listed here is the version we are working towards",
     "It is important in the release process that this repository is tagged and pushed and then a new commit made to advance the version"
  ],
  "name": "@folio/address-plugin-generic",
  "description": "Plugin to render a generic address form",
  "version": "1.5.0",
  "scripts": {
    "lint": "eslint ."
  },
  "dependencies": {
    "@folio/address-utils": "^1.2.0",
    "prop-types": "^15.6.0",
    "react": "^16.7.0",
    "react-flexbox-grid": "^2.1.2",
    "eslint": "^5.6.0"
  },
  "devDependencies": {
    "@folio/eslint-config-stripes": "^4.1.1",
    "babel-eslint": "^9.0.0",
    "eslint": "^5.6.0"
  },
  "peerDependencies": {
    "react-intl": ">4.0.0"
  }
}
